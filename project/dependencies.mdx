---
title: Dependencias / Dependencies
description: 'Dependencias / Dependencies - Kadoku Documentation'
---

<Tabs>
  <Tab title="Español">
# Gestión de Dependencias

Las dependencias se gestionan en el archivo `project.ku.json`. Puedes añadir paquetes de la librería estándar o del registro oficial de Kadoku usando `kadoku add`.
  </Tab>
  <Tab title="English">
# Dependency Management

Dependencies are managed in the `project.ku.json` file. You can add packages from the standard library or the official Kadoku registry using `kadoku add`.
  </Tab>
</Tabs>
